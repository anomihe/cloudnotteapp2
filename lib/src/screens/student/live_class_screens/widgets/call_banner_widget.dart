import 'package:flutter/material.dart';

class CallBannerWidget extends StatelessWidget {
  const CallBannerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 40,
      color: Colors.blue,
      child: Text("Banner Card"),
    );
  }
}
